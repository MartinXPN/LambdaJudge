{
  "code": {
    "main.py": "import encodings.idna; import json; import urllib.request; conditionsSetURL = 'http://httpbin.org/post'; newConditions = {'con1':40, 'con2':20, 'con3':99, 'con4':40, 'password':'1234'}; params = json.dumps(newConditions).encode('utf8'); req = urllib.request.Request(conditionsSetURL, data=params, headers={'content-type': 'application/json'}); response = urllib.request.urlopen(req); print(response.read().decode('utf8'))\n"
  },
  "programInputs": ["nothing"],
  "language": "python",
  "timeLimit": 5,
  "memory_limit": 500
}